%form.download{:action => download_path(:file => file), :method => :post}
  = hidden_field_tag :authenticity_token, form_authenticity_token
  %button{:type => :submit}
    %i.fa.fa-download
    = label
